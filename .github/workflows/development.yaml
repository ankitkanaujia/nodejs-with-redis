name: First Workflow

on: push 

jobs: 
  build: 
    runs-on: ubuntu-latest  
    steps: 
     -name: GET CODE
      uses: actions/checkout@v3 
     -name: Install dependencies
      run: npm ci     