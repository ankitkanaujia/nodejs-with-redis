name: First Workflow

on: push 

jobs: 
  build: 
    runs-on: ubuntu-latest  
    strategy:
      matrix:
        node-version: [16.x]

    steps: 
     -name: Get Code 
      uses: actions/checkout@v3 
     -name: Use node js
      uses: actions/setup-node@v3
     -name: Install dependencies
      run: npm ci     